import 'package:flutter/material.dart';

class BranchProfile extends StatelessWidget {
  const BranchProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("BRANCH Profile"),
        centerTitle: true,
      ),
    );
  }
}
